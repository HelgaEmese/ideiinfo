#include <iostream>
#include <fstream>
using namespace std;

int main()
{ int n,i,v[1000],min,max,a;
    fstream in("input.txt");
    in>>n;
     in>>i;
    int min=i, max=i;
    while (in>>i){
        if (min>i){
            min=i;
        }
        if(max<i)
            max=i;
    }
  int temp_min =min;
  int hatvany =1;
  while

    cout << << endl;
    return 0;
}
